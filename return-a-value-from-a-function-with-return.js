function discountedPrice(num) {
    return num - 3;
}

function normalPrice(num) {
    return num + 5;
}

console.log("The price is now " + discountedPrice(10));
console.log("The price is now " + normalPrice(5));

